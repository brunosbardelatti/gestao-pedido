[build]
builder = "NIXPACKS"
buildCommand = "npm ci && npm run build --workspace @gestao-pedidos/frontend"

[deploy]
startCommand = "npm run start --workspace @gestao-pedidos/frontend"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
